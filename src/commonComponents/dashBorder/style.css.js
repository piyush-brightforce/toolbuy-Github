import {StyleSheet} from 'react-native';
const styles = StyleSheet.create({
  separator: {
    height: 1,
    overflow: 'hidden',
  },
  separatorLine: {
    height: 2,
    borderWidth: 1.6,
    borderColor: '#ECEEF2',
    borderStyle: 'dashed',
  },
});

export default styles;
